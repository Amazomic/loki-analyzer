services:
  log-analyzer:
    build:
      context: .
      dockerfile: Dockerfile.yml
    ports:
      - "8080:80"
    environment:
      - API_KEY=${GEMINI_API_KEY}
    restart: always

  # Optional: Local Loki for testing
  # loki:
  #   image: grafana/loki:latest
  #   ports:
  #     - "3100:3100"
  #   command: -config.file=/etc/loki/local-config.yaml
